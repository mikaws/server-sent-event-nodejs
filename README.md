# Node.js Server Sent Event (SSE) - Client/Server


## Getting Started
- Run `node index.js` in each folder to start both applications.

- Access http://localhost:8000 in your web browser to view the page.

<br/>

**By Mikaws | Michael Dias**